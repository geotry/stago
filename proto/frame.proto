syntax = "proto3";

package pubs;

option go_package = "./pb";

// Represents a single frame
message Frame {
  bytes data = 1;
  uint32 number = 2;
  Format format = 3;
}

enum Format {
  RAW = 0;
  RLE = 1;
  DELTA = 2;
}
